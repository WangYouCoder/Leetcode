#include <iostream>
#include <string>
using namespace std;
string s;
int main()
{
 cin >> s;
 for(int i = 0; i < s.size(); i++)
 {
 if(s[i] % 2 == 0) s[i] = '0';
 else s[i] = '1';
 }
 cout << stoi(s) << endl; // ⾃动处理前导零
 return 0;
}
